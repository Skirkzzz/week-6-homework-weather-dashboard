class UI {
    constructor() {
        this.uiContainer = document.getElementById("content");
        this.city;
    )

    (method) UI.populateUI(data: any): void
    populateUI(data) {
        //destructure variables
        //add them to inner HTML

        this.uiContainer.innerHTML = '

        <
        div class = "card mx-auto mt-5"
        '
    }
}
}